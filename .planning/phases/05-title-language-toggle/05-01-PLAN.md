---
phase: 05-title-language-toggle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SearchBar.tsx
  - src/components/AnimeCard.tsx
  - src/App.tsx
  - src/types/anime.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Toggle button appears in search bar area"
    - "When enabled, Japanese title displays (title_japanese or title_english)"
    - "When disabled, default English title displays (title)"
    - "Preference persists in localStorage across sessions"
    - "Toggle works in both search results and anime detail modal"
  artifacts:
    - path: "src/components/SearchBar.tsx"
      provides: "Toggle button UI with language toggle state"
      contains: "toggle button with icon"
    - path: "src/components/AnimeCard.tsx"
      provides: "Title display in search results"
      contains: "title prop that respects language toggle"
    - path: "src/App.tsx"
      provides: "Detail modal and search bar integration"
      contains: "useTitleLanguageToggle hook, localStorage, dialog title"
  key_links:
    - from: "SearchBar.tsx"
      to: "App.tsx"
      via: "onToggle callback prop"
      pattern: "onTitleLanguageToggle"
    - from: "AnimeCard.tsx"
      to: "App.tsx"
      via: "titleLanguage prop"
      pattern: "titleLanguage.*prop"
---

<objective>
Add a Title Language Toggle feature that allows users to switch between English and Japanese titles in search results and the anime detail modal.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/types/anime.ts
@src/components/SearchBar.tsx
@src/components/AnimeCard.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create title language hook with localStorage persistence</name>
  <files>src/hooks/useTitleLanguageToggle.ts</files>
  <action>Create a new hook `useTitleLanguageToggle.ts` in src/hooks/ that:
- Exports function `useTitleLanguageToggle()` returning `[isJapanese: boolean, toggle: () => void]`
- Reads/writes to localStorage key `anime-tracker-title-language` with values `'japanese'` | `'english'`
- Defaults to `'english'` if no preference stored
- Uses useState with lazy initial value to read localStorage only once on mount</action>
  <verify>File exists at src/hooks/useTitleLanguageToggle.ts with useState and localStorage.getItem/setItem calls</verify>
  <done>Hook created and exported. Ready to use in SearchBar and App components.</done>
</task>

<task type="auto">
  <name>Task 2: Add toggle button to SearchBar component</name>
  <files>src/components/SearchBar.tsx</files>
  <action>Modify SearchBarProps interface to accept new optional prop: `titleLanguage?: 'english' | 'japanese'; onTitleLanguageToggle?: () => void`. Add a toggle button inside the search bar container (after the input field, before the clear button):
- Use a small icon button with Japanese flag (日本語) text or globe icon
- Style: `text-lg px-2 py-1 ml-2 rounded bg-[var(--color-surface)] text-[var(--color-foreground-muted)] hover:text-[var(--color-foreground)]`
- Show "EN" when titleLanguage is "japanese", show "JP" when disabled
- When clicked, call onTitleLanguageToggle() if provided</action>
  <verify>Toggle button appears next to search input. Clicking it calls onTitleLanguageToggle callback.</verify>
  <done>Toggle button appears in search bar next to clear button or loading spinner.</done>
</task>

<task type="auto">
  <name>Task 3: Update AnimeCard to use title language preference</name>
  <files>src/components/AnimeCard.tsx</files>
  <action>Modify AnimeCardProps interface to accept new optional prop: `titleLanguage?: 'english' | 'japanese'`. Update the title rendering logic:
- When `titleLanguage === 'japanese'`: display `anime.title_japanese || anime.title_english || anime.title`
- When `titleLanguage === 'english'` or undefined: display `anime.title` (current behavior)
- The API returns: title (string), title_english (string | null), title_japanese (string | null)</action>
  <verify>Anime cards display Japanese title when toggle is enabled, English when disabled.</verify>
  <done>AnimeCard shows correct title based on titleLanguage prop.</done>
</task>

<task type="auto">
  <name>Task 4: Wire toggle through App and SearchResults</name>
  <files>src/App.tsx, src/components/SearchResults.tsx</files>
  <action>In App.tsx:
- Import and use the `useTitleLanguageToggle` hook
- Pass `titleLanguage={isJapanese ? 'japanese' : 'english'}` and `onTitleLanguageToggle={toggle}` to SearchBar
- Pass `titleLanguage` to SearchResults via prop

In SearchResults.tsx:
- Update prop interface to accept `titleLanguage?: 'english' | 'japanese'`
- Pass through to each AnimeCard component</action>
  <verify>Toggle in search bar controls title language in all search result cards.</verify>
  <done>Toggle fully wired from SearchBar through SearchResults to AnimeCard.</done>
</task>

<task type="auto">
  <name>Task 5: Update anime detail modal in App.tsx</name>
  <files>src/App.tsx</files>
  <action>In App.tsx, update the DialogTitle in the anime detail modal to respect title language:
- Pass `titleLanguage` prop to dialog or use inline logic
- Use same logic as AnimeCard: `title_japanese || title_english || title` when Japanese, `title` when English
- Also update the dialog image alt text similarly</action>
  <verify>Anime detail modal shows Japanese title when toggle is enabled.</verify>
  <done>Detail modal displays correct title based on language preference.</done>
</task>

<task type="auto">
  <name>Task 6: Final verification</name>
  <files>src/App.tsx</files>
  <action>Run a build to verify no TypeScript errors. Test the complete flow:
1. Page loads with English titles by default
2. Click toggle button in search bar
3. Search for anime - titles should now show in Japanese
4. Click on anime card to open detail modal - should show Japanese title
5. Refresh page - preference persists (Japanese titles still show)
6. Click toggle again - returns to English titles</action>
  <verify>npm run build passes without errors. Toggle works end-to-end.</verify>
  <done>Feature complete: toggle works in search results AND detail modal, persists in localStorage.</done>
</task>

</tasks>

<verification>
- [ ] Build passes without TypeScript errors
- [ ] Toggle button appears in search bar area
- [ ] Clicking toggle switches title language in search results
- [ ] Clicking anime card opens detail modal with same title language
- [ ] Refreshing page preserves language preference
- [ ] Both EN→JP and JP→EN toggle works correctly
</verification>

<success_criteria>
Toggle button visible in search bar, Japanese titles display when enabled, English titles display when disabled, preference persists across page refresh, works in both search results and detail modal.
</success_criteria>

<output>
After completion, create `.planning/phases/05-title-language-toggle/05-01-SUMMARY.md`
</output>