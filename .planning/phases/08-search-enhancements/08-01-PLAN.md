---
phase: 08-search-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api.ts
  - src/hooks/useSearch.ts
  - src/components/SearchBar.tsx
autonomous: true
requirements:
  - SECH-05
  - SECH-06

must_haves:
  truths:
    - User can filter search results by anime type (TV, Movie, OVA, Special, ONA, Music)
    - User can choose what shows when search tab has no query (Popular/Latest/Seasonal)
    - Category and default view selections persist during session only
  artifacts:
    - path: src/lib/api.ts
      contains: type parameter in getTopAnime and searchAnime
    - path: src/hooks/useSearch.ts
      contains: category filter state and defaultView state
    - path: src/components/SearchBar.tsx
      contains: category badges and default view selector
  key_links:
    - from: SearchBar.tsx
      to: useSearch hook
      via: category and defaultView props
    - from: useSearch hook
      to: api.ts
      via: category parameter passed to API calls
---

<objective>
Add category/type filters and default view options to the search tab per SECH-05 and SECH-06.
Output: Category filter badges, default view selector in search bar, updated API calls with type parameter.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/api.ts
@src/hooks/useSearch.ts
@src/components/SearchBar.tsx

Jikan API type parameter: type=tv|movie|ona|ova|special|music
Default views: Popular (/top/anime), Latest (currently airing), Seasonal (current season)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update api.ts with type parameter</name>
  <files>src/lib/api.ts</files>
  <action>
Add optional `type` parameter to both `getTopAnime` and `searchAnime` functions. When provided, append `&type={type}` to the API URL. Add new function `getSeasonalAnime(year, season)` for seasonal default view. Default view "Latest" maps to `/anime?status=airing&order_by=start_date&sort=desc` (currently airing), "Seasonal" maps to `/seasons/now`.

Per D-04: Session-only, no localStorage persistence.
</action>
  <verify>
<automated>MISSING — manual verification: console.log with different type values returns filtered results</automated>
  </verify>
  <done>getTopAnime(query, page, type) and searchAnime(query, page, type) accept type parameter, getSeasonalAnime available</done>
</task>

<task type="auto">
  <name>Task 2: Update useSearch with category and default view state</name>
  <files>src/hooks/useSearch.ts</files>
  <action>
Extend useSearch hook to manage:
- `category`: state for selected anime type filter (defaults to empty/undefined for all)
- `setCategory`: setter for category filter
- `defaultView`: state for default view preference ('popular' | 'latest' | 'seasonal', defaults to 'popular')
- `setDefaultView`: setter for default view preference

Pass category to both getTopAnime and searchAnime calls. Handle default view logic in `loadTopAnime` — when query is empty and defaultView is 'latest', call appropriate API; when 'seasonal', call getSeasonalAnime.

Per D-04: State resets on app close (React state, no localStorage).
</action>
  <verify>
<automated>console.log(category, defaultView) updates in hook</automated>
  </verify>
  <done>useSearch exposes category, setCategory, defaultView, setDefaultView</done>
</task>

<task type="auto">
  <name>Task 3: Add filter badges and default view selector to SearchBar</name>
  <files>src/components/SearchBar.tsx</files>
  <action>
Extend SearchBarProps to accept:
- `category`: current category filter
- `onCategoryChange`: callback when category changes
- `defaultView`: current default view
- `onDefaultViewChange`: callback when default view changes

Add category filter badges (TV, Movie, OVA, Special, ONA, Music) on right side of search bar next to language toggle. Badge shows active state with accent color when selected, inactive state with muted color. Clicking active badge clears filter.

Add simple dropdown or toggle group for default view (Popular/Latest/Seasonal) below or next to category badges.

Per D-01: Inline filter badges on right side of search bar. Per D-02: User configures via direct toggle in search area.
</action>
  <verify>
<automated>Clicking badge updates category in useSearch, clicking default view updates defaultView</automated>
  </verify>
  <done>Category badges visible and functional, default view selector visible and functional</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jikan API → client | Untrusted external API returns anime data, validate type parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---------|----------|-----------|-----------|-------------|
| T-08-01 | Denial of Service | api.ts | accept | Type parameter validation: only allow tv\|movie\|ona\|ova\|special\|music |
| T-08-02 | Information Disclosure | api.ts | accept | API returns public anime data only, no PII |
</threat_model>

<verification>
[ ] Category badges render in SearchBar
[ ] Clicking category badge updates useSearch state
[ ] API receives type parameter and returns filtered results
[ ] Default view selector renders
[ ] Changing default view loads appropriate content on empty query
[ ] Filter resets when active category badge clicked again
</verification>

<success_criteria>
- Category filter badges (TV, Movie, OVA, Special, ONA, Music) visible in search bar
- Selecting a category filters search results by that type
- Default view selector (Popular/Latest/Seasonal) visible in search bar
- Selecting default view changes what loads on empty query
- Both selections persist during session only (no saved state)
</success_criteria>

<output>
After completion, create `.planning/phases/08-search-enhancements/08-01-SUMMARY.md`
</output>