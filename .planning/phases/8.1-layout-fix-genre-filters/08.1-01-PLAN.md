---
phase: 8.1-layout-fix-genre-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/api.ts, src/hooks/useSearch.ts, src/components/SearchResults.tsx, src/components/SearchBar.tsx]
autonomous: true
requirements: [UX-07, UX-08]
user_setup: []

must_haves:
  truths:
    - "User can see genre filter badges in right panel on lg+ screens"
    - "User can click a genre to filter anime by that genre"
    - "Clicking another genre deselects the previous one (single-select)"
    - "Cards are left-aligned on the grid"
    - "Right panel is 260px wide with sticky positioning"
    - "Mobile (<1024px) shows floating Filters button instead of sidebar"
  artifacts:
    - path: "src/lib/api.ts"
      provides: "getGenres() function fetching from Jikan /genres/anime"
      contains: "export interface Genre"
    - path: "src/hooks/useSearch.ts"
      provides: "genre state, selectedGenreId, setGenre, genres list"
      contains: "genres: Genre[]"
    - path: "src/components/SearchResults.tsx"
      provides: "Right panel with genre badges, left-aligned card grid"
      contains: "lg:w-[260px]", "justify-start", "sticky"
    - path: "src/components/SearchBar.tsx"
      provides: "Filters moved to right panel, category badges simplified"
      contains: "Category filter moved to right panel"
  key_links:
    - from: "src/components/SearchResults.tsx"
      to: "/api/getGenres"
      via: "useEffect on mount"
      pattern: "useEffect.*getGenres"
    - from: "src/hooks/useSearch.ts"
      to: "/api/searchAnime|getTopAnime"
      via: "genre parameter"
      pattern: "genre.*mal_id"
---

<objective>
Implement genre filters with right sidebar layout and left-aligned cards.

Purpose: Provide ~45 genre filter badges from Jikan API in a right panel while keeping the main search area clean. Cards left-aligned for better visual flow.

Output: Working genre filter in right panel, left-aligned anime cards grid
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/8.1-layout-fix-genre-filters/CONTEXT.md

# Current structure reference
@src/lib/api.ts:
- getTopAnime(), searchAnime() with type parameter
@src/hooks/useSearch.ts:
- category filter already implemented (similar pattern for genre)
@src/components/SearchResults.tsx:
- current grid uses: `grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4` (centered)

@src/components/SearchBar.tsx:
- Category badges in search bar (to move to right panel)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add genre API function</name>
  <files>src/lib/api.ts</files>
  <action>
Add `getGenres()` function to fetch anime genres from Jikan API `/genres/anime` endpoint. Returns Genre[] with mal_id and name. Include fallback hardcoded list if API fails.

Interface:
```typescript
export interface Genre {
  mal_id: number;
  name: string;
}

export async function getGenres(): Promise<Genre[]>
```

Use rate limiting (350ms) like other API functions.
  </action>
  <verify>
    <automated>Check api.ts has getGenres exported</automated>
  </verify>
  <done>getGenres() function fetches from Jikan /genres/anime, cached in state after first load</done>
</task>

<task type="auto">
  <name>Task 2: Add genre state to useSearch hook</name>
  <files>src/hooks/useSearch.ts</files>
  <action>
Extend useSearch hook with genre filtering:
- Add `genres: Genre[]` state (loaded via getGenres on mount)
- Add `selectedGenreId: number | null` state for single-select
- Add `setGenre` function to set/clear genre filter
- Modify search API calls to include `genres` parameter (Jikan uses `genres` param with MAL ID)
- On init: call getGenres() to load genre list

Pattern: Follow same approach as category filter (session-only, not persisted).
  </action>
  <verify>
    <automated>Check useSearch.ts exports genre, selectedGenreId, setGenre</automated>
  </verify>
  <done>Hook provides genre state, fetches genres on init, filters by selectedGenreId</done>
</task>

<task type="auto">
  <name>Task 3: Restructure SearchResults with right panel</name>
  <files>src/components/SearchResults.tsx</files>
  <action>
Restructure layout with right sidebar:
1. **Container**: Add outer flex container with `justify-between`
2. **Main content**: Cards grid with `flex-1`, `grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 justify-start` (LEFT-ALIGN, not centered)
3. **Right panel**: 
   - Width: 260px fixed (`w-[260px]`)
   - Show only on lg+ screens (`hidden lg:block`)
   - Sticky positioning (`sticky top-4`)
   - Contains: Genre badges (scrollable if >15), category dropdown moved from SearchBar, default view selector, clear filters button
4. **Mobile**: Floating "Filters" button (lg:hidden), opens drawer/modal with genre + category + default view
5. **Genre badges**: Single-select (clicking one deselects others)

Import Genre interface from api.ts. Pass genre props from parent or useSearch directly.
  </action>
  <verify>
  <automated>Check SearchResults.tsx has right panel with w-[260px] and genre filter</automated>
  </verify>
  <done>Right panel shows genre badges on lg+, cards left-aligned, mobile has Filters button</done>
</task>

<task type="auto">
  <name>Task 4: Simplify SearchBar (move filters)</name>
  <files>src/components/SearchBar.tsx</files>
  <action>
Simplify SearchBar by moving filters to right panel:
1. Remove category badges (keep only search input + clear button)
2. Remove default view selector  
3. **Rationale**: CONTEXT.md UX-08 says right panel contains these - keep search bar focused on search input

Keep: Search input field, title language toggle, clear button, error display.
  </action>
  <verify>
    <automated>Check SearchBar.tsx has no category badges</automated>
  </verify>
  <done>SearchBar contains only search input field, simplified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| {client→Jikan API} | Untrusted external API response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8.1-01 | Denial of Service | getGenres() | accept | Rate limited to 350ms, cached after first load |
| T-8.1-02 | Invalid Data | Genre filter | mitigate | Validate genre mal_id before API call, ignore invalid |
| T-8.1-03 | Information Disclosure | External API | accept | No PII, public anime data only |
</threat_model>

<verification>
- [ ] Genre badges display in right panel on lg+ screens
- [ ] Clicking genre filters anime results
- [ ] Single-select works (clicking another deselects previous)
- [ ] Cards grid is left-aligned (not centered)
- [ ] Mobile shows floating "Filters" button
- [ ] Category filter moved from SearchBar to right panel
</verification>

<success_criteria>
Genre filter functional with single-select from right sidebar; left-aligned cards grid; responsive (right panel hidden on mobile with Filters button)
</success_criteria>

<output>
After completion, create `.planning/phases/8.1-layout-fix-genre-filters/08.1-01-SUMMARY.md`
</output>