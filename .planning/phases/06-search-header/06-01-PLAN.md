---
phase: 06-search-header
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/App.tsx, src/components/SearchBar.tsx, src/components/SearchHeader.tsx, src/hooks/useTitleLanguageToggle.ts]
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can toggle between English and Japanese titles"
    - "Search bar is in header with icon that expands to input"
    - "Home tab replaces Search tab"
    - "Title logo navigates to Home"
  artifacts:
    - path: "src/components/SearchHeader.tsx"
      provides: "Expandable search with icon in header"
---

<objective>
Phase 6: Search Header Redesign

Purpose: Fix title language toggle bug and redesign search bar to header with expandable input.
Output: Clean header with search icon, expandable input, language toggle, Home/My List tabs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/App.tsx (current layout)
@src/components/SearchBar.tsx
@src/hooks/useTitleLanguageToggle.ts

Theme: Cinema Dark (from CLAUDE.md)
- Background: #0a0a0f → #020203
- Accent: #5e6ad2
- Cards: glassmorphism
- Animations: 150-300ms
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix title language toggle bug</name>
  <files>src/App.tsx, src/hooks/useTitleLanguageToggle.ts</files>
  <read_first>src/App.tsx (lines 1-35), src/hooks/useTitleLanguageToggle.ts</read_first>
  <action>
The toggle function is being recreated on each render. Use useCallback to ensure stable reference:

In useTitleLanguageToggle.ts:
```tsx
import { useState, useCallback, useEffect } from 'react';

type TitleLanguage = 'english' | 'japanese';

const STORAGE_KEY = 'anime-tracker-title-language';

function getStoredLanguage(): TitleLanguage {
  if (typeof window === 'undefined') return 'english';
  const stored = localStorage.getItem(STORAGE_KEY);
  return (stored === 'japanese' || stored === 'english') ? stored : 'english';
}

export function useTitleLanguageToggle() {
  const [isJapanese, setIsJapanese] = useState<TitleLanguage>(() => getStoredLanguage());

  // Use useEffect to persist to localStorage instead of in the callback
  useEffect(() => {
    localStorage.setItem(STORAGE_KEY, isJapanese);
  }, [isJapanese]);

  const toggle = useCallback(() => {
    setIsJapanese(prev => prev === 'japanese' ? 'english' : 'japanese');
  }, []);

  return { isJapanese, toggle };
}
```
  </action>
  <verify>
    <automated>grep -n "useCallback\|useEffect" src/hooks/useTitleLanguageToggle.ts</automated>
  </verify>
  <done>Title language toggle switches between EN and JP correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create SearchHeader component</name>
  <files>src/components/SearchHeader.tsx</files>
  <action>
Create new SearchHeader component with expandable search:

```tsx
import { useState, useRef, useEffect } from 'react';

interface SearchHeaderProps {
  query: string;
  setQuery: (q: string) => void;
  loading?: boolean;
  isJapanese: boolean;
  onLanguageToggle: () => void;
}

export function SearchHeader({ query, setQuery, loading, isJapanese, onLanguageToggle }: SearchHeaderProps) {
  const [expanded, setExpanded] = useState(false);
  const inputRef = useRef<HTMLInputElement>(null);

  useEffect(() => {
    if (expanded && inputRef.current) {
      inputRef.current.focus();
    }
  }, [expanded]);

  const handleToggle = () => {
    setExpanded(!expanded);
  };

  const handleKeyDown = (e: React.KeyboardEvent) => {
    if (e.key === 'Escape') {
      setExpanded(false);
      setQuery('');
    }
  };

  return (
    <div className="relative flex items-center">
      <div className={`relative flex items-center transition-all duration-300 ${expanded ? 'w-64' : 'w-10'}`}>
        {expanded ? (
          <div className="relative w-full">
            <input
              ref={inputRef}
              type="text"
              placeholder="Search anime..."
              value={query}
              onChange={(e) => setQuery(e.target.value)}
              onKeyDown={handleKeyDown}
              className="w-full bg-[var(--color-surface)] text-[var(--color-foreground)] border border-[var(--color-border)] rounded-xl px-4 py-2 pr-20
                         placeholder-[var(--color-foreground-muted)] focus:outline-none focus:border-[var(--color-accent)] transition-all duration-200 text-sm"
            />
            <div className="absolute right-2 top-1/2 -translate-y-1/2 flex items-center gap-1">
              <button
                onClick={onLanguageToggle}
                className={`text-xs px-2 py-1 rounded font-medium transition-colors ${
                  isJapanese 
                    ? 'bg-[var(--color-accent)] text-white' 
                    : 'bg-[var(--color-bg-elevated)] text-[var(--color-foreground-muted)] hover:text-[var(--color-foreground)]'
                }`}
              >
                {isJapanese ? 'JP' : 'EN'}
              </button>
              {query.length > 0 && !loading && (
                <button
                  onClick={() => setQuery('')}
                  className="text-[var(--color-foreground-muted)] hover:text-[var(--color-foreground)] p-1"
                >
                  <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
                  </svg>
                </button>
              )}
              <button
                onClick={handleToggle}
                className="text-[var(--color-foreground-muted)] hover:text-[var(--color-foreground)] p-1"
              >
                <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
            </div>
          </div>
        ) : (
          <button
            onClick={handleToggle}
            className="w-10 h-10 flex items-center justify-center rounded-xl bg-[var(--color-surface)] text-[var(--color-foreground-muted)] hover:text-[var(--color-foreground)] hover:bg-[var(--color-surface-hover)] transition-all duration-200"
          >
            <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
          </button>
        )}
      </div>
    </div>
  );
}
```

Export from index if needed.
  </action>
  <verify>
    <automated>ls -la src/components/SearchHeader.tsx</automated>
  </verify>
  <done>SearchHeader component created with expandable search</done>
</task>

<task type="auto">
  <name>Task 3: Update App.tsx with new header layout</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx (lines 70-130)</read_first>
  <action>
Update App.tsx:

1. Import SearchHeader
2. Add onClick to h1 for navigation
3. Replace "Search" tab with "Home"
4. Add SearchHeader to header (far right, before tabs)
5. Remove SearchBar from main area

Changes:
- Add: import { SearchHeader } from './components/SearchHeader';
- Add: onClick to h1: onClick={() => setViewMode('search')}
- Change: "Search" button text → "Home"
- Add SearchHeader in header before the tabs div
- Remove SearchBar from main area (lines 114-123)
  </action>
  <verify>
    <automated>grep -n "SearchHeader\|Home\|onClick.*setViewMode" src/App.tsx</automated>
  </verify>
  <done>App.tsx has new header with SearchHeader, clickable title, Home tab</done>
</task>

<task type="auto">
  <name>Task 4: Verify build and test</name>
  <files>src/App.tsx</files>
  <action>
Run build to check for errors:
```bash
npm run build
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | head -30</automated>
  </verify>
  <done>Build passes without errors</done>
</task>

</tasks>

<verification>
- [ ] Title language toggle switches EN ↔ JP correctly
- [ ] Search icon expands to input with animation
- [ ] Input auto-focuses when expanded
- [ ] Language toggle visible in expanded search
- [ ] X button clears search
- [ ] Escape key closes search
- [ ] "Search" tab renamed to "Home"
- [ ] Title "Anime Tracker" is clickable and goes to Home
- [ ] Old SearchBar removed from below tabs
</verification>

<success_criteria>
- Toggle bug fixed - switches between EN and JP
- Header has: [Title (clickable)] → [SearchIcon] → [Home] [My List]
- Search expands with smooth animation
- Language toggle inside expanded search
- Old search bar removed
</success_criteria>

<output>
After completion, create `.planning/phases/06-search-header/06-01-SUMMARY.md`
</output>