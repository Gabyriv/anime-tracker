---
phase: 04-ui-ux-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SearchBar.tsx
  - src/components/Pagination.tsx
  - src/components/AnimeCard.tsx
  - src/components/UserListCard.tsx
  - src/components/ui/Dialog.tsx
  - src/App.tsx
  - src/context/ToastContext.tsx
  - src/hooks/useSearch.ts
autonomous: true
requirements:
  - UX-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - "Search tab scrolls to top when page changes"
    - "Browse mode and search mode have independent page counters"
    - "Modal scrolls without visible scrollbar"
    - "Show more/Show less has +/- icons"
    - "Badges have padding"
    - "Search bar has X button to clear input"
    - "UserListCard image takes full height of card"
    - "Toast background color matches status color"
    - "Toast text formats status properly (e.g., 'Completed' not 'completed')"
  artifacts:
    - path: "src/components/SearchBar.tsx"
      contains: "X button for clearing input"
    - path: "src/components/Pagination.tsx"
      contains: "scroll to top on page change"
    - path: "src/App.tsx"
      contains: "synopsisExpanded setState, +/- icons"
    - path: "src/components/ui/Dialog.tsx"
      contains: "scrollbar-hiding CSS"
    - path: "src/context/ToastContext.tsx"
      contains: "status-based background color"
    - path: "src/hooks/useSearch.ts"
      contains: "separate browsePage and searchPage states"
---

<objective>
Complete UI/UX improvements: pagination fixes, modal scrollbar fix, search bar X button, toast enhancements, and layout refinements.
</objective>

<context>
@.planning/phases/04-ui-ux-improvements/04-01-PLAN.md
@src/App.tsx
@src/components/SearchBar.tsx
@src/components/Pagination.tsx
@src/components/AnimeCard.tsx
@src/components/UserListCard.tsx
@src/components/ui/Dialog.tsx
@src/context/ToastContext.tsx
@src/hooks/useSearch.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scroll to top on page change</name>
  <files>src/components/Pagination.tsx</files>
  <action>Add window.scrollTo({ top: 0, behavior: 'smooth' }) in handlePrev, handleNext, and handlePageNum functions after calling onPageChange. This ensures the page scrolls to top when user changes pages.</action>
  <verify>
    <automated>grep -n "scrollTo" src/components/Pagination.tsx</automated>
  </verify>
  <done>Pagination component scrolls to top when page changes</done>
</task>

<task type="auto">
  <name>Task 2: Separate page state for browse vs search</name>
  <files>src/hooks/useSearch.ts</files>
  <action>Replace single 'page' state with 'browsePage' and 'searchPage'. Modify loadTopAnime to use browsePage, modify search effect to use searchPage. Add a 'mode' ref that tracks whether we're in browse or search mode, and switch page refs accordingly when query changes from empty to non-empty and vice versa.</action>
  <verify>
    <automated>grep -n "browsePage\|searchPage" src/hooks/useSearch.ts</automated>
  </verify>
  <done>Independent page counters for browse and search modes</done>
</task>

<task type="auto">
  <name>Task 3: Hide scrollbar in modal while allowing scroll</name>
  <files>src/components/ui/Dialog.tsx</files>
  <action>Update line 23: change 'overflow-y-auto' to use custom scrollbar-hiding CSS. Replace className with: 'relative z-50 bg-[var(--color-bg-elevated)] border border-[var(--color-border)] rounded-2xl p-6 pt-12 max-w-2xl w-full max-h-[80vh] overflow-y-auto scrollbar-hide'. Add scrollbar-hide class to Tailwind config or use inline style with '::-webkit-scrollbar { display: none; }' and 'scrollbar-width: none'.</action>
  <verify>
    <automated>grep -n "scrollbar\|scrollbar-hide" src/components/ui/Dialog.tsx</automated>
  </verify>
  <done>Modal scrolls content but scrollbar is hidden</done>
</task>

<task type="auto">
  <name>Task 4: Add +/- icons to Show more/Show less</name>
  <files>src/App.tsx</files>
  <action>Modify lines 192-197 in App.tsx: Replace text 'Show more' with icon + text 'Show more' and 'Show less' with icon + text 'Show less'. Button content: `{synopsisExpanded ? '− Show less' : '+ Show more'}` with styled spans for the +/- symbols.</action>
  <verify>
    <automated>grep -n "Show more\|Show less\|−\|+" src/App.tsx | head -20</automated>
  </verify>
  <done>Show more/Show less button has +/- icons</done>
</task>

<task type="auto">
  <name>Task 5: Add padding to badges in AnimeCard</name>
  <files>src/components/AnimeCard.tsx</files>
  <action>Modify lines 42-46 in AnimeCard.tsx: Add px-2 py-0.5 to each badge span. Change from `{year && <span>{year}</span>}` to `{year && <span className="px-2 py-0.5">{year}</span>}`. Apply same padding to episodes and score badges.</action>
  <verify>
    <automated>grep -n "px-2 py" src/components/AnimeCard.tsx</automated>
  </verify>
  <done>Badges (year, episodes, score) have consistent padding</done>
</task>

<task type="auto">
  <name>Task 6: Add X button to clear search input</name>
  <files>src/components/SearchBar.tsx</files>
  <action>After the input element (line 22), add a conditional X button when query has text: `{query && query.length > 0 && ( <button onClick={() => setQuery('')} className="...">×</button> )}`. Position it absolute right-4 top-1/2 -translate-y-1/2. Use clear icon with hover effect showing when there's input.</action>
  <verify>
    <automated>grep -n "setQuery\|clear" src/components/SearchBar.tsx</automated>
  </verify>
  <done>Search bar shows X button when input has text</done>
</task>

<task type="auto">
  <name>Task 7: Make UserListCard image fill card height</name>
  <files>src/components/UserListCard.tsx</files>
  <action>Modify lines 63-73 in UserListCard.tsx: Change img from 'w-24 h-36' to 'w-auto h-full min-h-[144px] max-w-[96px]'. The height stays as h-36 (144px) but width becomes proportional (auto with max constraint of 96px) to fill the card's left side naturally while maintaining aspect ratio.</action>
  <verify>
    <automated>grep -n "w-24\|w-auto\|h-full" src/components/UserListCard.tsx</automated>
  </verify>
  <done>UserListCard image takes full height of left side</done>
</task>

<task type="auto">
  <name>Task 8: Toast background color based on status</name>
  <files>src/context/ToastContext.tsx</files>
  <action>Modify ToastContext to accept status-based colors. Update the addToast function to accept optional status: 'watching' | 'completed' | 'plan_to_watch' | 'on_hold' | 'dropped'. Map to colors: completed=#22c55e (green), watching=#3b82f6 (blue), plan_to_watch=#eab308 (yellow), on_hold=#f97316 (orange), dropped=#ef4444 (red). Apply as bg-color class on the toast element.</action>
  <verify>
    <automated>grep -n "status_completed\|status_watching\|#22c55e\|#3b82f6" src/context/ToastContext.tsx</automated>
  </verify>
  <done>Toast background color matches status</done>
</task>

<task type="auto">
  <name>Task 9: Format toast text properly</name>
  <files>src/App.tsx</files>
  <action>Find where handleStatusChange calls addToast. Format the message as: 'Status Updated to {statusLabel}' with capitalized statusLabel: 'Watching', 'Completed', 'Plan to Watch', 'On Hold', 'Dropped'. Use proper capitalization and title case in the message string.</action>
  <verify>
    <automated>grep -n "Status Updated\|Status updated" src/App.tsx</automated>
  </verify>
  <done>Toast shows properly formatted status text</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| client→Jikan API | Untrusted input from search, rate limited |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Toast messages | accept | No sensitive data in toasts |
| T-04-02 | Denial of Service | Rapid page changes | accept | Pagination already debounced |
</threat_model>

<verification>
- [ ] Test pagination scrolls to top
- [ ] Test browse vs search have separate page counters
- [ ] Test modal scrolls without visible scrollbar
- [ ] Test Show more/Show less has +/- icons
- [ ] Test badges have padding in search results
- [ ] Test X button clears search input
- [ ] Test UserListCard image fills height
- [ ] Test toast background color matches status
- [ ] Test toast text formatting
</verification>

<success_criteria>
All 9 tasks implemented. Search pagination works with scroll-to-top. Browse and search modes track pages independently. Modal scrolls but scrollbar hidden. All UI enhancements complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-ux-improvements/{phase}-02-SUMMARY.md`
</output>