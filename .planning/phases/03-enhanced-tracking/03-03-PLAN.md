---
phase: 03-enhanced-tracking
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/hooks/useAnimeList.ts
  - src/components/UserList.tsx
autonomous: true
requirements:
  - ENHT-01
  - ENHT-02
  - ENHT-03
---

<objective>
Wire new edit handlers from useAnimeList to UserList component.

Purpose: Connect the new database functions through the hook to the UI, enabling the full save flow.
Output: UserList passes edit handlers to UserListCard, changes save to database
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/hooks/useAnimeList.ts
@src/components/UserList.tsx

## New database functions from plan 03-01:
- updateEpisodeProgress(entryId, episodes)
- updatePersonalRating(entryId, rating | null)
- updatePersonalNotes(entryId, notes | null)

## UserListCard needs these props (from plan 03-02):
- onEpisodeChange: (entryId: number, episodes: number) => void
- onRatingChange: (entryId: number, rating: number | null) => void
- onNotesChange: (entryId: number, notes: string | null) => void
- totalEpisodes: number | null (from entry.episodes)
</context>

<tasks>

<task type="auto">
<parameter name>Task 1: Add update functions to useAnimeList hook</name>
<files>src/hooks/useAnimeList.ts</files>
<action>
Update useAnimeList.ts to add three new functions:
- Import updateEpisodeProgress, updatePersonalRating, updatePersonalNotes from db.ts
- Add updateEpisodeProgress: (entryId: number, episodes: number) => Promise<void>
- Add updatePersonalRating: (entryId: number, rating: number | null) => Promise<void>
- Add updatePersonalNotes: (entryId: number, notes: string | null) => Promise<void>

Each function should call the db function, then refresh() the list.
</action>
  <verify>
Hook exports three new update functions that call db and refresh
</verify>
  <done>Hook provides methods to update episode, rating, notes</done>
</task>

<task type="auto">
<name>Task 2: Wire handlers in UserList component</name>
<files>src/components/UserList.tsx</files>
<action>
Update UserList.tsx:
- Destructure the three new update functions from useAnimeList hook
- Pass them to each UserListCard as props:
  - onEpisodeChange: handleEpisodeChange
  - onRatingChange: handleRatingChange  
  - onNotesChange: handleNotesChange
- Also pass totalEpisodes: entry.episodes

Create handler functions that call the hook methods and refresh.
</action>
  <verify>
UserList passes all new props to UserListCard, edits save to database
</verify>
  <done>Full flow: user edits in card → saves to database</done>
</task>

</tasks>

<verification>
- useAnimeList exports new update functions
- UserList wires handlers to UserListCard
- Edit changes persist to database
</verification>

<success_criteria>
User can edit episode progress, rating, and notes with changes persisting to database
</success_criteria>

<output>
After completion, create `.planning/phases/03-enhanced-tracking/03-03-SUMMARY.md`
</output>