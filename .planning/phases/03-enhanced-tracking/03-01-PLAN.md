---
phase: 03-enhanced-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
autonomous: true
requirements:
  - ENHT-01
  - ENHT-02
  - ENHT-03
---

<objective>
Add database functions for updating episode progress, personal rating, and personal notes.

Purpose: These database updates are needed before UI can save the user edits.
Output: New update functions in db.ts
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/lib/db.ts

## Existing database structure:
- user_list table has: episodes_watched, personal_rating, personal_notes columns
- Current: updateStatus function exists at line 151

## Type from src/types/anime.ts:
```typescript
export interface UserAnimeEntry {
  id: number;
  anime_id: number;
  status: AnimeStatus;
  episodes_watched: number;
  personal_rating: number | null;
  personal_notes: string | null;
  updated_at: string;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add updateEpisodeProgress function</name>
  <files>src/lib/db.ts</files>
  <action>
Add updateEpisodeProgress function after updateStatus function (around line 157). Function should:
- Accept entryId and episodes count
- UPDATE user_list SET episodes_watched = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?
- Call saveDb() after update
- Use prepared statements with ? placeholders
  </action>
  <verify>
File has function with signature: export async function updateEpisodeProgress(entryId: number, episodes: number): Promise<void>
  </verify>
  <done>episodes_watched can be updated for any user list entry</done>
</task>

<task type="auto">
  <name>Task 2: Add updatePersonalRating function</name>
  <files>src/lib/db.ts</files>
  <action>
Add updatePersonalRating function after updateEpisodeProgress. Function should:
- Accept entryId and rating (1-10, or null to clear)
- UPDATE user_list SET personal_rating = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?
- Call saveDb() after update
- Use prepared statements with ? placeholders
  </action>
  <verify>
File has function with signature: export async function updatePersonalRating(entryId: number, rating: number | null): Promise<void>
  </verify>
  <done>personal_rating can be updated for any user list entry</done>
</task>

<task type="auto">
  <name>Task 3: Add updatePersonalNotes function</name>
  <files>src/lib/db.ts</files>
  <action>
Add updatePersonalNotes function after updatePersonalRating. Function should:
- Accept entryId and notes string (or null to clear)
- UPDATE user_list SET personal_notes = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?
- Call saveDb() after update
- Use prepared statements with ? placeholders
  </action>
  <verify>
File has function with signature: export async function updatePersonalNotes(entryId: number, notes: string | null): Promise<void>
  </verify>
  <done>personal_notes can be updated for any user list entry</done>
</task>

</tasks>

<verification>
All three functions exist in db.ts with proper async signatures and saveDb() calls.
</verification>

<success_criteria>
Database functions added: updateEpisodeProgress, updatePersonalRating, updatePersonalNotes
</success_criteria>

<output>
After completion, create `.planning/phases/03-enhanced-tracking/03-01-SUMMARY.md`
</output>