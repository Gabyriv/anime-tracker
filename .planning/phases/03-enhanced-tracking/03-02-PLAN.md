---
phase: 03-enhanced-tracking
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/UserListCard.tsx
  - src/hooks/useAnimeList.ts
autonomous: true
requirements:
  - ENHT-01
  - ENHT-02
  - ENHT-03
---

<objective>
Extend UserListCard with inline editing for episode progress, star rating, and notes.

Purpose: Users can directly edit these fields on each card in their list.
Output: Enhanced UserListCard component with editable fields
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/components/UserListCard.tsx

## User decisions (from CONTEXT.md):
- D-01: Use inline number input for episode progress
- D-02: Use clickable star rating (1-10 stars)
- D-03: Use expandable textarea for notes
- D-04: Auto-save on change

## New props needed:
- onEpisodeChange: (entryId: number, episodes: number) => void
- onRatingChange: (entryId: number, rating: number | null) => void
- onNotesChange: (entryId: number, notes: string | null) => void
- totalEpisodes: number | null (for display format "X / Y")
</context>

<tasks>

<task type="auto">
<parameter name>Task 1: Add onEpisodeChange, onRatingChange, onNotesChange props</name>
<files>src/components/UserListCard.tsx</files>
<action>
Update UserListCardProps interface to add:
- onEpisodeChange: (entryId: number, episodes: number) => void
- onRatingChange: (entryId: number, rating: number | null) => void
- onNotesChange: (entryId: number, notes: string | null) => void

Update destructured props to include these new handlers.
</action>
  <verify>
Props interface includes all three new handler functions
</verify>
  <done>Component accepts edit handlers as props</done>
</task>

<task type="auto">
<name>Task 2: Add inline episode progress input</name>
<files>src/components/UserListCard.tsx</files>
<action>
After the title and year display (around line 53), add episode progress display/input:
- Show "X / Y episodes" format if totalEpisodes is known and > 0
- Show "Ep X" format if totalEpisodes unknown or null
- Use number input with min=0, step=1, className for inline editing
- Call onEpisodeChange(entry.id, value) on change (auto-save per D-04)
- Display only when status is 'watching' (or always - decide: always shows progress)
</action>
  <verify>
Card displays episode count and allows inline editing
</verify>
  <done>Episode progress can be updated inline on each card</done>
</task>

<task type="auto">
<name>Task 3: Add clickable star rating (1-10)</name>
<files>src/components/UserListCard.tsx</files>
<action>
After episode progress display, add star rating component:
- Display current rating as yellow stars if set, gray if not set
- On click, show rating picker or cycle through 1-10
- Per D-02: clickable star rating UI - consider simple approach: click to cycle through 1-10 then clear
- On rating selection, call onRatingChange(entry.id, rating)
- Auto-save on change per D-04
</action>
  <verify>
Star rating displays and allows click to set 1-10
</verify>
  <done>Personal rating can be set via clickable stars</done>
</task>

<task type="auto">
<name>Task 4: Add expandable textarea for notes</name>
<files>src/components/UserListCard.tsx</files>
<action>
Add notes section after rating:
- Per D-03: expandable textarea on the card
- Show small collapsed view with "Add note..." placeholder if empty
- On focus, expand to show textarea
- On blur/change, call onNotesChange(entry.id, value)
- Auto-save per D-04
- Space-efficient: maybe just a small expandable area
</action>
  <verify>
Notes textarea expands on focus and saves on change
</verify>
  <done>Personal notes can be added and edited inline</done>
</task>

</tasks>

<verification>
UserListCard has all three new edit features: episode input, star rating, expandable notes
</verification>

<success_criteria>
User can edit episode progress, rating, and notes directly on each list card
</success_criteria>

<output>
After completion, create `.planning/phases/03-enhanced-tracking/03-02-SUMMARY.md`
</output>