---
phase: 02-search-list-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/anime.ts
  - src/lib/api.ts
  - src/hooks/useSearch.ts
autonomous: true
requirements:
  - SECH-01
  - SECH-02

must_haves:
  truths:
    - User can type anime title and see results within 1 second
    - Results display with image, title, and year
    - Search debounces to prevent excessive API calls
  artifacts:
    - path: src/lib/api.ts
      provides: Jikan API fetch wrapper
      exports: searchAnime( query: string ): Promise<AnimeFromApi[]>
    - path: src/hooks/useSearch.ts
      provides: Debounced search hook
      exports: useSearch(): { query, setQuery, results, loading, error }
    - path: src/components/SearchBar.tsx
      provides: Search input component with debounce
  key_links:
    - from: SearchBar.tsx
      to: useSearch hook
      via: useState + useEffect with 500ms delay
    - from: useSearch
      to: api.ts
      via: fetch call to jikan API
---

<objective>
Implement anime search functionality with Jikan API integration

Purpose: Users can search for anime by title and see results displayed in a grid card layout
Output: Search component with type-ahead behavior and debounced API calls
</objective>

<context>
@.planning/phases/02-search-list-management/02-CONTEXT.md
@.planning/phases/02-search-list-management/02-RESEARCH.md
@src/lib/db.ts
@src/types/anime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Jikan API wrapper</name>
  <files>src/lib/api.ts</files>
  <read_first>src/types/anime.ts</read_first>
  <action>
Create src/lib/api.ts with:
- BASE_URL: 'https://api.jikan.moe/v4'
- async function searchAnime(query: string, limit = 25): Promise<AnimeFromApi[]>
- Returns empty array on error (catches network/fetch errors)
- URL encodes the query parameter
- Includes timeout handling (10 second abort)
  </action>
  <verify>
    <automated>grep -l "searchAnime" src/lib/api.ts && grep "jikan.moe" src/lib/api.ts</automated>
  </verify>
  <done>searchAnime('naruto') returns array of AnimeFromApi objects with title, images, year</done>
  <acceptance_criteria>
- [ ] src/lib/api.ts exists and exports searchAnime function
- [ ] Function calls https://api.jikan.moe/v4/anime?q={query}
- [ ] Returns Promise<AnimeFromApi[]>
- [ ] Handles errors gracefully (returns empty array, no throw)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create debounced search hook</name>
  <files>src/hooks/useSearch.ts</files>
  <read_first>src/lib/api.ts</read_first>
  <action>
Create src/hooks/useSearch.ts:
- Import useState, useEffect from React
- Import searchAnime from ../lib/api
- Export function useSearch() returning:
  - query: string (current search text)
  - setQuery: React.Dispatch (update query)
  - results: AnimeFromApi[] 
  - loading: boolean
  - error: string | null
- useEffect with 500ms debounce (per D-03)
- Calls searchAnime when query changes after debounce
- Clears results when query is empty
- Handles race conditions (ignores stale results)
  </action>
  <verify>
    <automated>grep -l "useSearch" src/hooks/useSearch.ts</automated>
  </verify>
  <done>Typing 'one' shows results after 500ms, results clear when input cleared</done>
  <acceptance_criteria>
- [ ] src/hooks/useSearch.ts exists
- [ ] Exports useSearch hook with query, setQuery, results, loading, error
- [ ] Debounce is exactly 500ms (D-03)
- [ ] Handles race conditions (latest query wins)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create SearchBar component</name>
  <files>src/components/SearchBar.tsx</files>
  <read_first>src/hooks/useSearch.ts</read_first>
  <action>
Create src/components/SearchBar.tsx:
- Import useSearch from ../hooks/useSearch
- Render input field with:
  - placeholder="Search anime..."
  - value={query}
  - onChange={e => setQuery(e.target.value)}
- Show loading spinner when loading=true
- Show error message in red when error is set
- Style: bg-gray-800, text-white, rounded, padding, dark border
- Full width of container
  </action>
  <verify>
<automated>grep -l "SearchBar" src/components/SearchBar.tsx</automated>
  </verify>
  <done>SearchBar renders input, typing triggers debounced search, loading shows during fetch</done>
  <acceptance_criteria>
- [ ] src/components/SearchBar.tsx exists
- [ ] Uses useSearch hook
- [ ] Shows loading state during API call
- [ ] Shows error message on API failure
- [ ] Styled with TailwindCSS (bg-gray-800, text-white pattern)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. SearchBar appears in UI with input field
2. Typing "naruto" shows results after 500ms
3. Results display anime cards with image, title, year
4. Loading spinner shows during fetch
5. Error message displays on failed request
</verification>

<success_criteria>
- User can type anime title and see results within 1 second
- Results display in grid cards with image, title, year
- Debounce prevents excessive API calls (500ms delay)
- Loading and error states are visible
</success_criteria>

<output>
After completion, create .planning/phases/02-search-list-management/02-01-SUMMARY.md
</output>