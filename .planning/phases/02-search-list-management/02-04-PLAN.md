---
phase: 02-search-list-management
plan: 04
type: execute
wave: 2
depends_on:
  - 02-03
files_modified:
  - src/components/UserList.tsx
  - src/components/UserListCard.tsx
  - src/components/StatusFilter.tsx
  - src/App.tsx
autonomous: true
requirements:
  - LIST-03
  - LIST-04

must_haves:
  truths:
    - User can view their complete anime list
    - User can filter list by status category
    - Entries are grouped by status (D-04)
  artifacts:
    - path: src/components/UserList.tsx
      provides: Main user list display component
    - path: src/components/UserListCard.tsx
      provides: Card for entries in user's list
    - path: src/components/StatusFilter.tsx
      provides: Filter tabs for status categories
  key_links:
    - from: UserList.tsx
      to: useAnimeList
      via: Uses list from hook
    - from: StatusFilter
      to: UserList
      via: Filter state passed as prop
---

<objective>
Build user list view with status filtering and grouping

Purpose: Users can view their anime list, filter by status, see entries grouped by category
Output: Filterable, grouped list display with status tabs
</objective>

<context>
@.planning/phases/02-search-list-management/02-CONTEXT.md
@src/hooks/useAnimeList.ts
@src/components/StatusDropdown.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusFilter tabs</name>
  <files>src/components/StatusFilter.tsx</files>
  <read_first>src/types/anime.ts</read_first>
  <action>
Create src/components/StatusFilter.tsx:
- Props: 
  - currentFilter: AnimeStatus | 'all'
  - onFilterChange: (filter: AnimeStatus | 'all') => void
  - counts?: Record<AnimeStatus | 'all', number> (optional, for badge counts)
- Options (tabs):
  - "All" - shows everything
  - "Watching"
  - "Completed"  
  - "Plan to Watch"
  - "On Hold"
  - "Dropped"
- Style: Horizontal tabs, active tab highlighted
- Per D-04: grouped display in mind (tabs for quick filtering)
  </action>
  <verify>
<automated>grep -l "StatusFilter" src/components/StatusFilter.tsx</automated>
  </verify>
  <done>StatusFilter shows 6 tabs, clicking changes filter</done>
  <acceptance_criteria>
- [ ] src/components/StatusFilter.tsx exists
- [ ] 6 options: All, Watching, Completed, Plan to Watch, On Hold, Dropped
- [ ] Calls onFilterChange with selected filter
- [ ] Visual active state for selected tab
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create UserListCard component</name>
  <files>src/components/UserListCard.tsx</files>
  <read_first>src/types/anime.ts</read_first>
  <action>
Create src/components/UserListCard.tsx:
- Props:
  - entry: UserAnimeEntry with anime data joined
  - onStatusChange: (status: AnimeStatus) => void
  - onRemove: () => void
- Display: image, title, year, status badge, rating if set
- Actions:
  - Status dropdown to change status (reuse StatusDropdown)
  - Remove button (trash icon)
- Style: Similar to AnimeCard but for user's list entries
  </action>
  <verify>
<automated>grep -l "UserListCard" src/components/UserListCard.tsx</automated>
  </verify>
  <done>UserListCard shows entry data with status badge and action buttons</done>
  <acceptance_criteria>
- [ ] src/components/UserListCard.tsx exists
- [ ] Shows anime image, title, year
- [ ] Shows current status as colored badge
- [ ] Has status change dropdown
- [ ] Has remove button
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create UserList main component</name>
  <files>src/components/UserList.tsx</files>
  <read_first>src/hooks/useAnimeList.ts</read_first>
  <action>
Create src/components/UserList.tsx:
- Props: (none - uses useAnimeList hook internally)
- State: currentFilter (AnimeStatus | 'all'), default 'all'
- Uses useAnimeList hook to get list
- Filter display:
  - Per D-04: Group entries by status into sections
  - OR use StatusFilter tabs for filtering
- Layout options:
  - Grouped sections: "Watching", "Completed", etc. (per D-04)
  - Or filtered view with StatusFilter tabs
- Empty state: "Your list is empty. Search for anime to add!"
- Uses existing Tailwind styles
  </action>
  <verify>
<automated>grep -l "UserList" src/components/UserList.tsx</automated>
  </verify>
  <done>UserList displays user's anime with grouping or filtering</done>
  <acceptance_criteria>
- [ ] src/components/UserList.tsx exists
- [ ] Displays all user list entries
- [ ] Groups by status per D-04 (or provides filter)
- [ ] Empty state when list is empty
- [ ] Uses useAnimeList hook
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Integrate UserList into App</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>
Update src/App.tsx:
- Import UserList from ./components/UserList
- Add tabs or toggle between "Search" and "My List" views
- State: activeView: 'search' | 'list'
- Header: Show both SearchBar (when on search view) and view toggle
- When activeView === 'list': show UserList component
- Persist view toggle in UI (simple button/tab switcher)
  </action>
  <verify>
<automated>grep "UserList" src/App.tsx</automated>
  </verify>
  <done>App shows toggle between search and my list views</done>
  <acceptance_criteria>
- [ ] App has view toggle between Search and My List
- [ ] My List view shows UserList component
- [ ] User can filter by status in My List
- [ ] Entries grouped by status per D-04
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. App shows "My List" tab/button
2. Clicking shows UserList component
3. Entries grouped by status sections (D-04)
4. Status filter tabs work
5. Clicking entry shows options to change status or remove
</verification>

<success_criteria>
- User can view their anime list (LIST-03)
- List is filtered by status (LIST-04)
- Entries grouped per D-04
- Can change status of entries
- Can remove entries
</success_criteria>

<output>
After completion, create .planning/phases/02-search-list-management/02-04-SUMMARY.md
</output>