---
phase: 02-search-list-management
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/hooks/useAnimeList.ts
  - src/components/StatusDropdown.tsx
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - SECH-04

must_haves:
  truths:
    - User can add anime to their personal list with a status
    - Duplicate entries are prevented
  artifacts:
    - path: src/hooks/useAnimeList.ts
      provides: CRUD operations for user's anime list
    - path: src/components/StatusDropdown.tsx
      provides: Status selection dropdown for anime cards
  key_links:
    - from: StatusDropdown
      to: useAnimeList
      via: addToList function call
    - from: useAnimeList
      to: db.ts
      via: SQL queries for anime_list + user_list
---

<objective>
Implement user list management - add anime to personal list with status

Purpose: Users can add anime from search results to their personal list with a status category
Output: Add to list functionality with dropdown status selector and duplicate prevention
</objective>

<context>
@.planning/phases/02-search-list-management/02-CONTEXT.md
@src/lib/db.ts
@src/types/anime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend database with list operations</name>
  <files>src/lib/db.ts</files>
  <read_first>src/lib/db.ts</read_first>
  <action>
Update src/lib/db.ts with new functions:
- getUserList(): Promise<UserAnimeEntry[]>
  - Returns all entries from user_list joined with anime_list
  - Include: id, anime_id, mal_id, title, image_url, status, episodes_watched, personal_rating

- addToList(anime: AnimeFromApi, status: AnimeStatus): Promise<number>
  - Insert into anime_list if not exists (ON CONFLICT DO NOTHING for mal_id)
  - Insert into user_list with given status
  - Return the user_list id
  - Throw error if already exists (for duplicate detection)

- updateStatus(entryId: number, status: AnimeStatus): Promise<void>
  - UPDATE user_list SET status = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?

- removeFromList(entryId: number): Promise<void>
  - DELETE FROM user_list WHERE id = ?
  - Optionally clean up anime_list if no longer referenced (deferred)

- getAnimeByMalId(malId: number): AnimeInDb | null
  - Helper: query anime_list by mal_id for duplicate checking
  </action>
  <verify>
<automated>grep "addToList\|getUserList\|updateStatus\|removeFromList" src/lib/db.ts</automated>
  </verify>
  <done>addToList stores anime in both tables, getUserList retrieves all entries</done>
  <acceptance_criteria>
- [ ] getUserList() returns all user anime entries with anime data
- [ ] addToList() inserts into both anime_list and user_list
- [ ] addToList() handles duplicate mal_id gracefully
- [ ] updateStatus() changes status in database
- [ ] removeFromList() deletes from user_list
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create useAnimeList hook</name>
  <files>src/hooks/useAnimeList.ts</files>
  <read_first>src/lib/db.ts</read_first>
  <action>
Create src/hooks/useAnimeList.ts:
- Import getDb, getUserList, addToList, updateStatus, removeFromList from ../lib/db
- Import AnimeStatus from ../types/anime
- Export function useAnimeList() returning:
  - list: UserAnimeEntry[] (with anime data joined)
  - loading: boolean
  - error: string | null
  - addToList: (anime: AnimeFromApi, status: AnimeStatus) => Promise<boolean>
  - updateStatus: (entryId: number, status: AnimeStatus) => Promise<void>
  - removeFromList: (entryId: number) => Promise<void>
  - refresh: () => void (reload list)
- useEffect to load initial list on mount
- addToList returns true if added, false if duplicate (caller shows message)
  </action>
  <verify>
<automated>grep -l "useAnimeList" src/hooks/useAnimeList.ts</automated>
  </verify>
  <done>useAnimeList provides list data and CRUD operations</done>
  <acceptance_criteria>
- [ ] src/hooks/useAnimeList.ts exists
- [ ] Exports useAnimeList hook
- [ ] Provides: list, addToList, updateStatus, removeFromList, refresh
- [ ] Handles duplicate gracefully (returns false, no throw)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Create StatusDropdown component</name>
  <files>src/components/StatusDropdown.tsx</files>
  <read_first>src/types/anime.ts</read_first>
  <action>
Create src/components/StatusDropdown.tsx:
- Props: 
  - currentStatus: AnimeStatus | null
  - onStatusChange: (status: AnimeStatus) => void
  - isInList: boolean (if already added)
- Options (per D-05 - dropdown on card):
  - "Add to list" (if not in list) → opens status options
  - "Watching", "Completed", "Plan to Watch", "On Hold", "Dropped"
- Display current status with colored badge if in list
- Dropdown: simple select or custom styled menu
- Style: compact, positioned on card (per D-05)
- When status selected: call onStatusChange
  </action>
  <verify>
<automated>grep -l "StatusDropdown" src/components/StatusDropdown.tsx</automated>
  </verify>
  <done>StatusDropdown shows status or "Add" button, changes trigger callback</done>
  <acceptance_criteria>
- [ ] src/components/StatusDropdown.tsx exists
- [ ] Shows 5 status options (Watching, Completed, Plan to Watch, On Hold, Dropped)
- [ ] Calls onStatusChange when status selected
- [ ] Shows current status as badge if anime is in list
- [ ] Dropdown on card per D-05
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. AnimeCard shows StatusDropdown
2. Selecting status adds anime to user list
3. Adding duplicate shows "Already in list" message
4. Database persists the entry
</verification>

<success_criteria>
- User can add anime to list with status (LIST-01)
- Duplicate entries prevented (LIST-06)
- Status stored in database
- Dropdown UX per D-05
</success_criteria>

<output>
After completion, create .planning/phases/02-search-list-management/02-03-SUMMARY.md
</output>