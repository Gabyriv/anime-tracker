---
phase: 02-search-list-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AnimeCard.tsx
  - src/components/SearchResults.tsx
  - src/App.tsx
autonomous: true
requirements:
  - SECH-03
  - SECH-04

must_haves:
  truths:
    - Search results display in grid cards with image, title, year
    - User can click/expand to see anime details
  artifacts:
    - path: src/components/AnimeCard.tsx
      provides: Card component for single anime result
    - path: src/components/SearchResults.tsx
      provides: Grid container for anime results
  key_links:
    - from: SearchResults.tsx
      to: AnimeCard.tsx
      via: Maps over results array
---

<objective>
Display search results in grid card layout with details view

Purpose: Users can browse search results and view anime details
Output: Grid of anime cards with click-to-expand details
</objective>

<context>
@.planning/phases/02-search-list-management/02-CONTEXT.md
@src/types/anime.ts
@src/components/SearchBar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnimeCard component</name>
  <files>src/components/AnimeCard.tsx</files>
  <read_first>src/types/anime.ts</read_first>
  <action>
Create src/components/AnimeCard.tsx:
- Props: anime: AnimeFromApi, onSelect?: (anime) => void
- Display: image (from anime.images.jpg.small_image_url), title, year
- Image: 120px width, aspect ratio preserved, rounded corners
- Title: truncate to 2 lines with ellipsis if too long
- Year: display next to title if available
- On click: call onSelect with full anime object
- Style per D-01: grid card layout with image + title below
- Hover: subtle scale transform (scale-105), transition
  </action>
  <verify>
<automated>grep -l "AnimeCard" src/components/AnimeCard.tsx</automated>
  </verify>
  <done>AnimeCard displays image, title, year; click triggers onSelect</done>
  <acceptance_criteria>
- [ ] src/components/AnimeCard.tsx exists
- [ ] Shows anime.images.jpg.small_image_url as image
- [ ] Shows anime.title text
- [ ] Shows anime.year if available
- [ ] Has onSelect callback prop
- [ ] Grid card layout per D-01
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create SearchResults grid component</name>
  <files>src/components/SearchResults.tsx</files>
  <read_first>src/components/AnimeCard.tsx</read_first>
  <action>
Create src/components/SearchResults.tsx:
- Props: results: AnimeFromApi[], onAnimeSelect: (anime) => void
- Layout: CSS Grid with responsive columns
  - Mobile: 2 columns
  - Tablet (md): 3 columns  
  - Desktop (lg): 4 columns
- Gap: 16px between cards
- Map over results and render AnimeCard for each
- Empty state: "No anime found" message when results is empty array
- Style: p-4 container
  </action>
  <verify>
<automated>grep -l "SearchResults" src/components/SearchResults.tsx</automated>
  </verify>
  <done>SearchResults renders grid of AnimeCards, responsive columns, empty state</done>
  <acceptance_criteria>
- [ ] src/components/SearchResults.tsx exists
- [ ] Grid layout: 2 cols mobile, 3 md, 4 lg
- [ ] Renders AnimeCard for each result
- [ ] Shows "No anime found" when results empty
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate search into App</name>
  <files>src/App.tsx</files>
  <read_first>src/App.tsx</read_first>
  <action>
Update src/App.tsx:
- Import SearchBar from ./components/SearchBar
- Import SearchResults from ./components/SearchResults
- Import useSearch from ./hooks/useSearch
- State: selectedAnime for details view (nullable)
- Layout:
  - Header with title "Anime Tracker"
  - SearchBar component
  - SearchResults component showing useSearch.results
- Handle selectedAnime display:
  - Show details in a modal or expanded section
  - Details to show: synopsis, episodes, score, status, year
  - Include "Add to List" button (for later plan)
- Use existing Tailwind classes: bg-gray-900, text-white
  </action>
  <verify>
<automated>grep "SearchBar\|SearchResults" src/App.tsx</automated>
  </verify>
  <done>App displays SearchBar, typing shows SearchResults grid, clicking shows details</done>
  <acceptance_criteria>
- [ ] src/App.tsx imports and renders SearchBar
- [ ] src/App.tsx imports and renders SearchResults
- [ ] Search results display in grid cards (D-01)
- [ ] Click on card shows details (synopsis, episodes, score, status, year)
- [ ] Uses existing Tailwind styles (bg-gray-900, text-white)
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. App renders with SearchBar at top
2. Typing search query shows grid of results
3. Grid is responsive (2/3/4 columns)
4. Each card shows image, title, year
5. Clicking card shows details overlay/modal
6. Details show: synopsis, episodes, score, status, year
</verification>

<success_criteria>
- Search results display in grid cards (D-01)
- Cards show image, title, year
- Clicking shows full anime details
- Responsive layout works on mobile/tablet/desktop
</success_criteria>

<output>
After completion, create .planning/phases/02-search-list-management/02-02-SUMMARY.md
</output>