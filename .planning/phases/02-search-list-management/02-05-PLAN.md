---
phase: 02-search-list-management
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/SearchBar.tsx
  - src/components/SearchResults.tsx
  - src/components/UserList.tsx
  - src/lib/api.ts
autonomous: true
requirements:
  - SECH-04

must_haves:
  truths:
    - Search handles API errors gracefully with user-friendly messages
    - App shows appropriate messages for various error conditions
  artifacts:
    - Error display in SearchBar
    - Empty state messages
    - Loading states
  key_links:
    - from: useSearch
      to: SearchBar
      via: error state passed to component
---

<objective>
Add error handling and empty states for search and list operations

Purpose: App handles API errors, empty results, and network issues gracefully with clear messages
Output: User-friendly error messages and empty state displays
</objective>

<context>
@.planning/phases/02-search-list-management/02-CONTEXT.md
@src/hooks/useSearch.ts
@src/hooks/useAnimeList.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance error handling in API</name>
  <files>src/lib/api.ts</files>
  <read_first>src/lib/api.ts</read_first>
  <action>
Update src/lib/api.ts:
- Add error handling for different failure types:
  - Network error: "Unable to connect. Check your internet connection."
  - Rate limit (429): "Too many requests. Please wait a moment."
  - API timeout: "Search timed out. Please try again."
  - Invalid response: "Received invalid data from server."
- Export error types or message constants
- Return descriptive error string instead of empty array on failure
- Include HTTP status code in error detection
  </action>
  <verify>
<automated>grep "rate.limit\|timeout\|network" src/lib/api.ts</automated>
  </verify>
  <done>API returns descriptive errors for different failure types</done>
  <acceptance_criteria>
- [ ] Network errors show "Unable to connect" message
- [ ] Rate limit (429) shows "Too many requests" message
- [ ] Timeout shows "timed out" message
- [ ] Errors passed to UI for display
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Improve error display in SearchBar</name>
  <files>src/components/SearchBar.tsx</files>
  <read_first>src/components/SearchBar.tsx</read_first>
  <action>
Update src/components/SearchBar.tsx:
- Show error message prominently in red (text-red-400)
- Include retry hint in error: "Try again" button or link
- Error state persists until next successful search
- When error occurs: keep previous results visible if any
- Add "Clear" button to reset search state
  </action>
  <verify>
<automated>grep "error\|text-red" src/components/SearchBar.tsx</automated>
  </verify>
  <done>SearchBar shows formatted error with retry option</done>
  <acceptance_criteria>
- [ ] Error displays in red text
- [ ] Error message is user-friendly (not technical)
- [ ] User can retry after error
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add empty states</name>
  <files>src/components/SearchResults.tsx</files>
  <read_first>src/components/SearchResults.tsx</read_first>
  <action>
Update src/components/SearchResults.tsx:
- Empty query state: "Start typing to search for anime"
- No results state: "No anime found for '{query}'"
- Loading state: Show skeleton cards or spinner (already in SearchBar)
- Style: Centered text, muted color, icon if appropriate
- For UserList empty: "Your list is empty. Search for anime to add!"
  </action>
  <verify>
<automated>grep "empty\|No anime found\|Your list is empty" src/components/SearchResults.tsx</automated>
  </verify>
  <done>SearchResults shows appropriate empty state messages</done>
  <acceptance_criteria>
- [ ] Shows "Start typing" when query is empty
- [ ] Shows "No anime found" when results empty
- [ ] UserList shows empty message when no entries
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. Type invalid query - shows "No anime found"
2. Disconnect network, search - shows "Unable to connect"
3. Rapid searches (rate limit) - shows "Too many requests"
4. Empty list - shows "Your list is empty" message
</verification>

<success_criteria>
- Error messages are user-friendly (SECH-04)
- Empty states guide user on what to do
- Loading states visible during API calls
- No silent failures - user always knows current state
</success_criteria>

<output>
After completion, create .planning/phases/02-search-list-management/02-05-SUMMARY.md
</output>